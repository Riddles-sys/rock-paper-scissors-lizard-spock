// import React from 'react'






// export default function Main({ buttonClicked, selection }) {
//   return <button onClick={() => buttonClicked(selection)}>{selection}</button>
// }

// export default function Main(props) {
//   return <button onClick={() => props.buttonClicked(props.selection)}>{props.selection}</button>
// }

{/* <Main
                key={selection}
                buttonClicked={buttonClicked}
                selection={selection}
              /> */}